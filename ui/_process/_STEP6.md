# Крок 6 — протокол критики групи (для субагентів)

Робочий файл уроку 07. Мета — **знайти й описати**, не виправляти.

## Що робиш

1. Завантаж скіл **impeccable** (Skill tool, `impeccable`) з аргументом
   `critique <файли твоєї групи>` і пройди його сценарій `reference/critique.md`:
   Assessment A (дизайн-огляд) і Assessment B (детектор + браузер), синтез, знімок у
   `.impeccable/critique/`. Скіл лежить у `C:\Users\Katya\.claude\skills\impeccable\` —
   скрипти запускай звідти абсолютним шляхом, наприклад
   `node "C:/Users/Katya/.claude/skills/impeccable/scripts/detect.mjs" --json wireframes/norm.html`.
   Якщо підагенти недоступні — A і B послідовно, як дозволяє сценарій.
2. **Нічого не виправляй.** Жодних змін у `wireframes/`, `ui/`, `DESIGN.md`, `concept/`.
   Єдиний файл, який пишеш, — знімок критики в `.impeccable/critique/`.
3. **Користувача не питай** (крок сценарію «Ask the User» пропусти) — повертаєш звіт мені.

## Середовище

- Сервер уже запущено: `http://localhost:8765` (корінь репозиторію), сторінки —
  `http://localhost:8765/wireframes/<page>.html`. Не зупиняй його.
- Браузер: **тільки власна нова вкладка** (`tabs_create`), її `tabId` у кожен виклик —
  паралельно працюють інші агенти. Наприкінці закрий свою вкладку.
- Якщо запускаєш `live-server.mjs` для оверлея детектора — зупини його перед звітом.
- Ширини для огляду: 1440 і 375 (і 1024, якщо щось ламається між ними).

## Контекст, який треба знати, щоб не шуміти

Продукт: `PRODUCT.md`; мова: `DESIGN.md`; кіт: `ui/kit.css`, вітрина `ui/kit.html`;
тексти — канон `voice.md` / `microcopy.md` (не пропонуй переписувати текст «на смак» —
тільки якщо він ламає зрозумілість, і тоді з посиланням на правило voice.md).

**Службовий хром — не продукт, не критикуй:** дерево екранів ліворуч (`.wf-tree`),
смуга станів (`.wf-states`), анотація під підвалом (`.wf-doc`).

**Ухвалені рішення — не знахідки** (назви тільки якщо рішення щось реально ламає):
темна тема без світлих блоків · хроматика тільки `--signal` на збій/строк · фото
обладнання без людей і краєвидів, морська зелень у знімку (К1, Р3) · панелі з рамкою, не
дільники (Р1) · на екранах рішення фото замість схеми (Р2) · дисплей у вазі 500 ·
анімацій немає (вето) · `[TBD]`-слоти — свідомий чесний пропуск вигаданих фактів ·
гігієнічна четвірка (company, careers, insights, legal) свідомо мінімальна.

**Уже відомі знахідки** — якщо бачиш, познач «відоме #N», не описуй заново:
1. Шапка без межі й фону, бренд окремим рядком над навігацією.
2. Посилання підвалу підкреслені сірі 14px.
3. Дата новини — системний моно замість JetBrains Mono.
4. Панель величин у чотирьох виглядах (`.values`, `--page`, `--doc`, `--receipt`).
5. Мітка поля: 0.86rem (guided matching) проти 0.9rem (inquiry).
6. Стрілки на approval-materials на кнопках «назад»/«вбік».
7. Скелет approval-loading на всю ширину, не в рейці.
8. Смуга секції на careers ввімкнулась випадково, контент зсунутий на 36px.
9. Сітка диптиха на станах crew-access: відступ 60px замість 32.
10. Norm на ширині рівно 560px — проміжний стан.
11. Активний пункт навігації ніде не позначений.

## Звіт (українською, стисло)

**А. Підсумок:** оцінка Nielsen /40, вердикт «AI slop» одним реченням, 2–3 сильні сторони.

**Б. Таблиця знахідок** — кожен рядок одна проблема:

| # | Файл | Елемент (селектор або опис) | Що не так | Тип | Вага | Де лагодиться |
|---|---|---|---|---|---|---|

- **Тип:** візуал · UX · a11y · текст · стан · адаптив
- **Вага:** P0 (ламає задачу) · P1 (суттєво) · P2 (помітно) · P3 (дрібне)
- **Де лагодиться:** `kit: значення` (змінна в :root — рознесеться сама) ·
  `kit: розмітка` (клас/структура компонента — переносити на всі екрани) ·
  `екран` (тільки ця сторінка) · `рішення` (потрібне рішення користувача)
- Відомі знахідки — окремим коротким списком «підтверджено: #N, #M».

**В. Детектор:** скільки знахідок, які правила, які хибні (і чому).

**Г. Знімок:** шлях до файлу в `.impeccable/critique/`.
