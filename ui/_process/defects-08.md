# Дефекти після розколу токенів — урок 08, крок 6

**2026-09-17.** Перевірено `tokens/tokens.css`, `components/` (40 файлів), 28 сторінок `wireframes/`,
`ui/kit.html`, `wireframes/_chrome.css` і `_theme.js`. Метод: скрипти (оголошення ↔ вживання
токенів, літерали в класах, значення тем), живий скан контрасту кожного текстового елемента
в обох темах, `/impeccable audit` (детектор + ручна перевірка його спрацювань).
Таблиця — на пріоритети; **статус після правок (2026-09-17):** користувач обрав «усі P1 і P2», рішення —
опитуванням: C1 → gray-55 для всієї ролі · N1 → злити `--solo-tier-fill` у `--rule-rank` · N4 → звести
до 14px · світла тема → лишається тестом, не продуктом (C3 не виправляється).

| # | Статус |
|---|---|
| C1 | ✅ `--rule-rank` gray-42 → gray-55: 4.18:1 до ґрунту, 3.94:1 до панелі |
| N1 | ✅ `--solo-tier-fill` прибрано, `.orbit-row .bar` читає `--rule-rank`; `--rule-gap` лишився gray-42 |
| N4 | ✅ 0.94rem і 0.9375rem → `--font-size-14`; primitive `--font-size-15`, `--font-size-15-04` прибрано; скелет кнопки — `calc()` з метрик кнопки (47.5px, як сама кнопка) |
| C3 | ➖ не виправляється: тема — тест |
| I1 | ✅ записано в CLAUDE.md §20: прийнятно для прототипу, бандл на білді |
| I2 | ✅ `color-scheme: dark` у `:root`, `light` у блоці теми |
| D1 | ✅ CLAUDE.md §19–20, README (індекс + «Tokens»), DESIGN.md (значення + таблиця правок), `ui/inventory.md`, `visuals/README.md`, `.impeccable/design.json` |
| P3 | відкриті: N2, N3, M1, H1–H3, C2, I3–I5, D2 |

Перевірка після правок: 28 сторінок на 375px без горизонтального переповнення; змінились лише
задумані властивості (кегль мети/кнопок/навігації/панелей, колір межі рангу) і похідні від
них переноси.

## Що чисто (перевірено, дефектів 0)

| Перевірка | Результат |
|---|---|
| hex, `oklch()`, назва шрифту прямо в класі компонента | **0** у 40 файлах `components/` |
| компонент читає колір-primitive (`--gray-*`, `--red-*`, `--chevron-*`) повз semantic | **0** — колір-primitive вживаються тільки в `tokens.css` |
| semantic-токен без коментаря походження в `:root` | **0** з 22 |
| токен без жодного вживання · вживаний, але не оголошений | **0 · 0** (крім слотів `--icon`, `--photo`, `--pad`, `--chip-icon` — API компонента) |
| геометрія числом у компоненті | **0** значень — лишились тільки структурні (`0`, `1fr`, `100%`, `-1`, `repeat(3, …)`, `grid-column: 7`, `translateY(-200%)`) і 22 умови `@media` (CSS не приймає там `var()`) |
| екран підключає `ui/kit.css` або має власний `<style>` / `style=""` | **0 з 28** |
| контраст тексту нижче AA | **0** — 28 сторінок + вітрина × темна і світла |
| вигляд після переведення | **0 розбіжностей** — 84 звірки, 22 629 елементів (крок 4) |

## Таблиця дефектів

| # | P | Файл | Елемент | Що не так | Як виправити |
|---|---|---|---|---|---|
| **Дві назви однієї ролі** |||||||
| N1 | P2 | `tokens/tokens.css` | `--rule-rank` · `--rule-gap` · `--solo-tier-fill` | У **обох** темах посилаються на той самий primitive (gray-42 / gray-55). Тема — перевірка, чи ролі справді різні, — їх не розвела | (а) злити `--solo-tier-fill` у `--rule-rank` (заливка ярусу = лінія рангу, одне місце); `--rule-gap` лишити — порожній слот і обране розійдуться зі станами уроку 09 · (б) злити всі три · (в) лишити, дописати коментар «розходяться на уроці 09» |
| N2 | P3 | `tokens/tokens.css` | `--text` = `--action`, `--bg-ground` = `--on-action` | Той самий primitive в обох темах | Не дефект за суттю: дія — інверсія до ґрунту за визначенням. Дописати коментар «дорівнює за визначенням мови», не зливати |
| N3 | P3 | `tokens/tokens.css` | `--space-N` / `--size-N` | 11 значень записані двома іменами: 12 · 13 · 14 · 16 · 22 · 24 · 44 · 56 · 64px, плюс `--space-1`/`--stroke-1`, `--space-2`/`--radius-2`/`--stroke-2` | (а) лишити — різні виміри (проміжок ≠ розмір) · (б) одна шкала `--px-N` для обох — перейменування в усіх компонентах, вигляд не змінюється |
| N4 | P2 | `tokens/tokens.css` | `--font-size-15` (0.9375rem) · `--font-size-15-04` (0.94rem) | Дві назви майже одного кегля — незведений дрейф D8 | Рішення D8 з `critique.md` §6.2: звести до 0.9375rem (переноси на 375px) або назвати свідомо |
| **Semantic без коментаря** |||||||
| M1 | P3 | `tokens/tokens.css` | 17 перевизначень у `[data-theme="light"]` | Без коментаря, чому саме цей primitive (коментар походження є лише в `:root`) | Коротка примітка на рядок: пара контрасту або «дзеркало темної» |
| **Колір і шрифт повз токени (службове)** |||||||
| H1 | P3 | `wireframes/_chrome.css` | `--svc-bg` 0.16 · `--svc-ink` 0.72 · `--svc-line` 0.28; `font-family: ui-monospace, "Consolas", monospace` | Літерали в службовому хромі; трьох значень немає в палітрі | Шрифт → `var(--font-system-mono)` (той самий рядок, 0 змін). Кольори: лишити винятком хрому і записати, або звести до gray-17/78/30 (хром зрушить на відтінок) |
| H2 | P3 | `ui/kit.html` | `<style>` каркаса вітрини (~20 правил) + 65 `style=""` | Власний блок стилів з літералами `oklch()` і назвами шрифтів. `style=""` — демо токенів (читають `var()`), це нормально | Перенести `<style>` каркаса у `ui/_kit-chrome.css` на `--svc-*`, як `_chrome.css` |
| H3 | P3 | `ui/kit.html` | `.doc-*` у світлій темі | Службові підписи вітрини сірі 0.72 на світлому ґрунті (~1.9:1) — продукт читається, каркас ні | Разом з H2: власний темний фон каркаса, як у дерева екранів |
| **Контраст (не текст)** |||||||
| C1 | P1 | `tokens/tokens.css` | `--rule-rank` у темній: межа `.btn--ghost`, обраної `.match`, `.reg-row--lead` | 2.40:1 до ґрунту — межа кнопки-контуру нижче 3:1 (WCAG 1.4.11). Відомий A15 уроку 07; у світлій 4.45:1 | gray-55 у темній (4.18:1) — ранг стане помітно жорсткішим, **зміна вигляду** — рішення |
| C2 | P3 | `tokens/tokens.css` | `--solo-event-edge` у темній, `.notice--error` | 1.56:1 до панелі | Не носій стану (іконка + слово + тон заголовка) — лишити, записати |
| C3 | P2 | `tokens/tokens.css` | `--event` у світлій = red-34 | Подія на світлому майже чорнило: хроматичний сигнал зникає. red-68 — 3.1:1, для чіпа 12.8px замало | Лише якщо світлу тему лишають: окремий primitive події для світлого (нове значення) |
| **`/impeccable audit`** |||||||
| I1 | P2 | `components/index.css` | 40 `@import` | Перед першим малюванням 42 CSS-запити каскадом — блокує рендер. Для прототипу терпимо | На білді — бандл (Next.js збирає сам); у прототипі лишити, записати в CLAUDE.md |
| I2 | P2 | `tokens/tokens.css` | `:root` / `[data-theme="light"]` | Немає `color-scheme` (A13 уроку 07): скролбари, автозаповнення, нативний список `select` не знають, що ґрунт темний | `color-scheme: dark` у `:root`, `light` у блоці теми. Зачіпає лише нативні частини, звірка DOM їх не бачить |
| I3 | P3 | `wireframes/_theme.js` | тема й `prefers-color-scheme` | Бриф §12: dark-first мусить поважати системну схему; перемикач ігнорує її | Лише якщо тему лишають: стартове значення з `matchMedia` |
| I4 | P3 | `wireframes/_theme.js` | кнопка «Theme test: light/dark» | `aria-pressed` і підпис, що міняється, — стан оголошується двічі | Сталий підпис «Light theme test» + `aria-pressed` |
| I5 | P3 | `wireframes/_chrome.css` | `.wf-states` на guided matching 1440px | Кнопка теми переносить опис стану — смуга +15px (службове) | Скоротити опис або дозволити кнопці стати в рядок заголовка |
| I6 | — | детектор | em dash · single font · numbered markers | **Хибні спрацювання:** усі 30 em dash у продуктовому тексті — дозволений маркер `[TBD — no source]` (voice.md); решта — коментарі CSS і службові підписи. «Один шрифт» — детектор не йде за `@import`, JetBrains Mono є. Номери — лічильники в коментарях | Нічого |
| **Документи (фінал уроку)** |||||||
| D1 | P2 | `CLAUDE.md` §19 · `README.md` · `DESIGN.md` · `ui/inventory.md` · `visuals/README.md` | згадки `ui/kit.css` | Кіт видалено — посилання на неіснуючий файл, опис «пласкої» архітектури | Розділ «Токени» в CLAUDE.md, «Tokens» у README (за промптом), решту — на `tokens/` + `components/` |
| D2 | P3 | `DESIGN.md` | frontmatter `colors:` | Старі пласкі назви (`bg`, `surface`, `ink-2`…) не збігаються з токенами | Назви ролей + primitive, або примітка-відповідність |

**Відкладене рішеннями, не повторюю:** `--solo-*` (critique §6.1), дрейф D1–D9 (§6.2), P3 уроку 07
(`defects.md`) і R5–R18, E2–E22 (`critique.md`) — вигляд не змінився, отже й вони.

## Оцінка `/impeccable audit`

| # | Вимір | Бал | Головне |
|---|---|---|---|
| 1 | Accessibility | 3 | текст AA в обох темах; межа рангу 2.40:1 у темній (C1) |
| 2 | Performance | 3 | 40 `@import` каскадом (I1) |
| 3 | Responsive | 3 | рендер ідентичний уроку 07 — ті самі відкриті P3 |
| 4 | Theming | 4 | тема перемикається без жодної правки компонента; одна діра знайдена й закрита |
| 5 | Anti-patterns | 4 | спрацювання детектора хибні (I6) |
| | **Разом** | **17/20** | **Good** (урок 07 — 15/20) |

**P0 — 0 · P1 — 1 (C1) · P2 — 6 (N1, N4, C3, I1, I2, D1) · P3 — 11**
